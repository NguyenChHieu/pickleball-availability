export default function NotFound() {
  return (
    <main className="route-state">
      <h1>We could not load this share page.</h1>
      <p>Check the link or try the stable fallback page.</p>
    </main>
  );
}
