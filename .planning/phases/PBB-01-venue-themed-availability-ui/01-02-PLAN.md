---
phase: PBB-01-venue-themed-availability-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/next.config.ts
  - web/tsconfig.json
  - web/eslint.config.mjs
  - web/.env.example
  - web/app/layout.tsx
  - web/app/globals.css
autonomous: true
requirements:
  - VIEW-01
  - THEME-01
  - THEME-02
  - THEME-03
user_setup: []
must_haves:
  truths:
    - "A separate `web/` Next.js TypeScript app exists beside the current extension and backend."
    - "`web/` has its own scripts for dev, build, lint, and typecheck."
    - "`web/` uses only the approved standard Next.js package set in this plan."
    - "`web/.env.example` contains only `NEXT_PUBLIC_BACKEND_URL=http://localhost:8787` and no backend secrets."
    - "The base CSS establishes the UI-SPEC typography, color variables, focus rings, touch targets, and reduced-motion defaults before route/component work begins."
  artifacts:
    - path: "web/package.json"
      provides: "Isolated Next.js TypeScript app scripts and approved dependencies"
      contains: "dev, build, lint, typecheck"
    - path: "web/.env.example"
      provides: "Local backend URL configuration example"
    - path: "web/app/layout.tsx"
      provides: "Global app shell and metadata"
    - path: "web/app/globals.css"
      provides: "Baseline design tokens, reset, focus, and reduced-motion CSS"
  key_links:
    - from: "web/package.json"
      to: "web/package-lock.json"
      via: "npm install"
      pattern: "lockfile"
    - from: "web/app/layout.tsx"
      to: "web/app/globals.css"
      via: "global stylesheet import"
      pattern: "import './globals.css'"
---

<objective>
Create the isolated `web/` Next.js TypeScript app boundary for the polished ProPickle availability page.

Purpose: prepare a safe frontend workspace with scripts, config, baseline styling, and no secret or scraping surface before adding public DTO fetching and availability components.
Output: package files, TypeScript/ESLint/Next config, env example, root layout, and UI-SPEC-aligned global CSS foundation.
</objective>

<execution_context>
@C:/Users/nguye/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/nguye/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/notes/venue-themed-web-app-direction.md
@.planning/phases/PBB-01-venue-themed-availability-ui/01-CONTEXT.md
@.planning/phases/PBB-01-venue-themed-availability-ui/01-RESEARCH.md
@.planning/phases/PBB-01-venue-themed-availability-ui/01-PATTERNS.md
@.planning/phases/PBB-01-venue-themed-availability-ui/01-UI-SPEC.md
@.planning/phases/PBB-01-venue-themed-availability-ui/01-01-SUMMARY.md
@server/package.json
</context>

<requirements>
Addresses VIEW-01 by creating the frontend app that will host the share-token page. Addresses THEME-01, THEME-02, and THEME-03 by establishing the app boundary, theme-capable CSS variables, and reduced-motion defaults before components are added.

Out of scope: public DTO fetching, dynamic route rendering, day cards, booking links, optional hero polish, deployment migration, extension share-link behavior, direct Supabase access, sync-token headers, scraping, booking automation, login, payment, checkout, waiver, CAPTCHA, and access-control automation.
</requirements>

<truths>
- `web/` is separate from `extension/` and `server/`.
- The scaffold does not import from extension globals or backend internals.
- The package set is intentionally small and auditable.
- Later plans can render availability without changing the backend fallback route.
</truths>

## Package Legitimacy Audit

| Package | Role | Status | Install Boundary |
|---------|------|--------|------------------|
| next | Next.js framework | approved standard package | `web/package.json` only |
| react | React runtime | approved standard package | `web/package.json` only |
| react-dom | React DOM runtime | approved standard package | `web/package.json` only |
| typescript | TypeScript compiler | approved standard package | `web/package.json` devDependency only |
| eslint | Lint runner | approved standard package | `web/package.json` devDependency only |
| eslint-config-next | Next.js lint config | approved standard package | `web/package.json` devDependency only |
| @types/node | Node types | approved standard package | `web/package.json` devDependency only |
| @types/react | React types | approved standard package | `web/package.json` devDependency only |
| @types/react-dom | React DOM types | approved standard package | `web/package.json` devDependency only |

No shadcn, Tailwind, GSAP, Three.js, React Three Fiber, auth, payment, Supabase, browser-extension, scraping, or Playbypoint SDK packages are approved in this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold package, TypeScript, ESLint, and env files</name>
  <files>web/package.json, web/package-lock.json, web/next.config.ts, web/tsconfig.json, web/eslint.config.mjs, web/.env.example</files>
  <read_first>
    @AGENTS.md
    @.planning/phases/PBB-01-venue-themed-availability-ui/01-RESEARCH.md
    @.planning/phases/PBB-01-venue-themed-availability-ui/01-PATTERNS.md
    @server/package.json
  </read_first>
  <action>Create `web/` with a private `package.json` containing scripts `dev`, `build`, `lint`, and `typecheck`. Direct dependencies must be exactly `next`, `react`, and `react-dom`; dev dependencies must be limited to `typescript`, `eslint`, `eslint-config-next`, `@types/node`, `@types/react`, and `@types/react-dom`. Generate `web/package-lock.json` with `npm.cmd --prefix web install` after confirming the direct dependency list. Add minimal `web/next.config.ts`, strict `web/tsconfig.json` with an `@/*` path alias to `src/*`, current-format `web/eslint.config.mjs`, and `web/.env.example` containing only `NEXT_PUBLIC_BACKEND_URL=http://localhost:8787`.</action>
  <acceptance_criteria>`web/package.json` contains the four required scripts and no unapproved direct dependencies; `web/.env.example` contains no `SUPABASE`, `AVAILABILITY_SYNC_TOKEN`, `x-sync-token`, database URL, or secret-like key names; `npm.cmd --prefix web run typecheck` and `npm.cmd --prefix web run lint` execute successfully after install.</acceptance_criteria>
  <verify>
    <automated>npm.cmd --prefix web run typecheck</automated>
    <automated>npm.cmd --prefix web run lint</automated>
  </verify>
  <done>The isolated frontend package/config boundary exists with an auditable lockfile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add root layout and UI-SPEC baseline CSS</name>
  <files>web/app/layout.tsx, web/app/globals.css</files>
  <read_first>
    @.planning/phases/PBB-01-venue-themed-availability-ui/01-UI-SPEC.md
    @.planning/notes/venue-themed-web-app-direction.md
    @web/package.json
  </read_first>
  <action>Create `web/app/layout.tsx` with app metadata for ProPickle cached court availability and a global import of `./globals.css`. Create `web/app/globals.css` with `box-sizing: border-box`, system font stack, UI-SPEC fixed type scale foundations, black/white base colors, electric-blue action/focus accent, pickleball-green highlight, muted text/border/warning variables, 44px touch target helpers, visible focus rings, and `prefers-reduced-motion: reduce` rules that disable nonessential transitions. Do not add route-specific availability components in this plan.</action>
  <acceptance_criteria>`web/app/layout.tsx` imports `./globals.css`; CSS defines reusable variables for background, foreground, accent blue, pickleball green, muted text, border, warning, radius, focus ring, and reduced-motion handling; no route component, backend fetcher, or booking link is introduced yet.</acceptance_criteria>
  <verify>
    <automated>npm.cmd --prefix web run typecheck</automated>
    <automated>npm.cmd --prefix web run lint</automated>
  </verify>
  <done>Baseline web shell and design-token CSS are ready for the route/component plan.</done>
</task>

</tasks>

## Artifacts this phase produces

- `web/package.json` private Next.js app scripts and approved dependency boundary.
- `web/package-lock.json` lockfile.
- `web/next.config.ts`, `web/tsconfig.json`, and `web/eslint.config.mjs`.
- `web/.env.example` with `NEXT_PUBLIC_BACKEND_URL`.
- `web/app/layout.tsx` and `web/app/globals.css`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> `web/package-lock.json` | Package install resolves framework dependencies. |
| `.env.example` -> deploy/runtime config | Public frontend config documents backend URL expectations. |
| global CSS -> future content | Foundation styling affects readability, focus, and motion behavior for later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | `web/.env.example` | mitigate | Document only `NEXT_PUBLIC_BACKEND_URL`; no secrets, sync tokens, Supabase keys, or database URLs. |
| T-01-08 | Denial of Service | frontend package boundary | mitigate | Keep direct dependencies limited to standard Next/React/TypeScript/ESLint packages. |
| T-01-09 | Elevation of Privilege | scaffold | mitigate | Add no auth, booking, extension, scraping, payment, checkout, waiver, CAPTCHA, or access-control code. |
| T-01-SC | Tampering | npm install | mitigate | Commit `web/package-lock.json` and stop for review before adding any extra direct dependency. |
</threat_model>

<verification>
Run from `C:\Users\nguye\Downloads\propickle-buddy`:
- `npm.cmd --prefix web run typecheck`
- `npm.cmd --prefix web run lint`
- Source gate: inspect `web/package.json` direct dependencies and search `web/.env.example` for `SUPABASE`, `AVAILABILITY_SYNC_TOKEN`, `x-sync-token`, `DATABASE_URL`, and `SECRET`.
</verification>

<success_criteria>
Plan 02 is complete when `web/` exists as an isolated, linting/typechecking Next.js TypeScript app boundary with approved dependencies, a safe env example, and UI-SPEC baseline styling.
</success_criteria>

<output>
Create `.planning/phases/PBB-01-venue-themed-availability-ui/01-02-SUMMARY.md` when done.
</output>
